Pour afficher les trois algorithmes sur des examples il faut lancer le main_test.py avec python 3.
Pour afficher le temps d'éxecution de chaque algorithme il faiut lancer le main_complexity.py avec python 3.

Le code est fournit avec un rapport ainsi que les images utilisé dans le rapport.
De plus, le script de plot ainsi que la chaine de production des graphes dans le script launch.sh.
